spring:
#  config:
#    activate:
#      on-profile: test

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fillinv_ledger?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root

#  flyway:
#    enabled: true
#    baseline-on-migrate: true
#    locations: classpath:db/migration

  jpa:
    database: mysql
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        # N+1 문제를 1+1로 줄여주는 옵션 (페이징 불가능 문제 해결)
#        default_batch_fetch_size: 100

logging:
  level:
    org.hibernate.SQL: DEBUG
