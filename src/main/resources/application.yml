spring:
  application:
    name: lesson-ledger
  profiles:
    default: local

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
